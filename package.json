{
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "web:echo": "(cd apps/web/ && make echo)",
    "web:dev": "yarn workspace web dev",
    "web:build": "yarn workspace @credit-payment-tool/db generate && yarn workspace web build",
    "web:start": "yarn workspace web start",
    "electron:build:mac": "yarn workspace @credit-payment-tool/db generate && yarn workspace web electron:build:mac",
    "db:dev": "yarn workspace @credit-payment-tool/db dev",
    "db:deploy": "yarn workspace @credit-payment-tool/db deploy",
    "db:generate": "yarn workspace @credit-payment-tool/db generate",
    "db:seed": "yarn workspace @credit-payment-tool/db seed",
    "db:studio": "yarn workspace @credit-payment-tool/db studio",
    "ncu": "yarn workspace web ncu && yarn workspace @credit-payment-tool/db ncu",
    "clean": "find . -name 'node_modules' -type d  -prune | awk '{print \"rm -rf\", $0}' | sh",
    "ncuu": "yarn workspaces foreach -A run ncuu",
    "ncum": "yarn workspace web ncum && yarn workspace @credit-payment-tool/db ncum",
    "ncumu": "yarn workspaces foreach -A run ncumu"
  },
  "overrides": {
    "glob": "^11.1.0"
  },
  "engines": {
    "npm": "use yarn instead",
    "yarn": "4.12.0"
  },
  "packageManager": "yarn@4.12.0"
}
